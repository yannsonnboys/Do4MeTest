<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-md-6">
    <div class="thumbnail">
      <!-- Show image -->
      <%= image_tag @listing.image.url %>
    </div>
    <p><a href="#" class="btn btn-primary" role="button">See Reviews</a></p>
  </div>

  <div class="col-md-6">
    <h3><%= @listing.name %></h3>
    <h5>Price: <%= number_to_currency(@listing.price, :unit=>'€') %></h5>
    <p><%= @listing.description %></p>
  </div>
</div>

<%= link_to 'Edit', edit_listing_path(@listing), class: "btn btn-link" %> |
<%= link_to 'Back', listings_path, class: "btn btn-link" %>
